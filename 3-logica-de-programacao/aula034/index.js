for (let i = 400; i <= 500; i += 10) {
    console.log(`Linha ${i}`);
}

for (let i = 500; i >= 400; i -= 10) {
    console.log(`Linha ${i}`);
}

const frutas = ['Maçã', 'Pêra', 'Uva', 'Banana', 'Melão', 'Ameixa', 'Melancia'];

for (let i = 0; i < frutas.length; i++) {
    console.log(`Índice ${i}, ${frutas[i]}`);
}
